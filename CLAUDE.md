# Project AI Workflow

@AGENTS.md

## Claude Code Notes

- Use the project slash commands under `.claude/commands/` to activate repo-local skills quickly.
- The canonical skill definitions live under `skills/`.
- When a slash command points to a skill, read that `skills/<name>/SKILL.md` file before substantial work.
- Use `tasks/open/*.md` as the durable implementation anchor when a task brief exists.
- Use `bash scripts/skills/validate-skills.sh` after skill changes.

