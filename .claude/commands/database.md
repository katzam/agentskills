Use the repo-local `project-database` skill for the current task.

Steps:
1. Read `skills/project-database/SKILL.md`.
2. Announce that `project-database` is active.
3. Follow its database and migration guidance for the current request.

