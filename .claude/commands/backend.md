Use the repo-local `project-backend` skill for the current task.

Steps:
1. Read `skills/project-backend/SKILL.md`.
2. Announce that `project-backend` is active.
3. Follow its backend implementation guidance for the current request.

