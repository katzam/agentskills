# Requirement Source Manifest

Use this file to track canonical requirement sources and any generated mirrors used by the agent.

## Notes

- Keep canonical source files under `docs/requirements/source/docx/` if you use the DOCX mirror flow.
- Generated Markdown mirrors belong under `docs/requirements/source/markdown/`.
- Leave the table empty until the target app actually has requirement sources to register.

## Canonical `.docx` Sources

| Current source | Owner | Notes | Status | Planned managed copy |
| --- | --- | --- | --- | --- |

