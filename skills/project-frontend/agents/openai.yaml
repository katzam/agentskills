interface:
  display_name: "Project Frontend"
  short_description: "Frontend and UI work for the repo"
  default_prompt: "Use $project-frontend to implement or update frontend code and the required frontend validation."

policy:
  allow_implicit_invocation: true

